const { setCookie, getCookie } = require('../../lib/utils/cookie')


module.exports = function setStatus (res, queryFun, userId) {
   
  console.log('userId : ')
  console.log(userId)

  // let menuList = JSON.stringify(menuData)
  
  // setCookie(res, 'userId', userId)
  // setCookie(res, 'userName', userName)
  // setCookie(res, 'menuList', menuList)
}